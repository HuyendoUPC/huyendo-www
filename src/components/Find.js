import React, { Component, PropTypes } from 'react';
import { Button } from 'react-bootstrap';

class Find extends Component {
  render() {
    return (
      <Button bsStyle="primary" bsSize="large">Find Trip</Button>
    );
  }
};

export default Find;
